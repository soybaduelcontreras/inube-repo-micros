server:
 port: 8106

 spring:
   application:
     name: inube-registry-service

 eureka:
   client: 
     registrer-with-eureka: false
     fetch-registry: false
   server:
     wait-time-in-ms-when-sync-empy:

management:
  endpoints:
    web:
      exposure:
        include:  "*"
    endpoint:
      health:
        show-details: always

      
logging:
   level:
     org.springframework.cloud.config: DEBUG
     org.springframework.security: DEBUG

     org.springframework.web.reactive.funcion.client.ExchangeFuncions: DEBUG
